# Careship Tracking page

> The careship tracking package is screts free

## Build Setup

``` bash
# Run the command below to install the package
$ npm install careship_tracking

```

or 

``` bash
# Run the command below to install the package
$ yarn add careship_tracking

```